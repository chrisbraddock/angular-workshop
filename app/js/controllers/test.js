'use strict';

angular.module('EL').controller('TestController', function ($scope) {

    // model
    $scope.model = {
        test: 'this is a test'
    };

    // actions
    $scope.action = {

    };

});